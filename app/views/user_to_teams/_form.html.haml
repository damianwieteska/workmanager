= form_for @user_to_team do |f|
  - if @user_to_team.errors.any?
    #error_explanation
      %h2= "#{pluralize(@user_to_team.errors.count, "error")} prohibited this user_to_team from being saved:"
      %ul
        - @user_to_team.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :role
    = f.number_field :role
  .field
    = f.label :user
    = f.text_field :user
  .field
    = f.label :team
    = f.text_field :team
  .actions
    = f.submit 'Save'
