.list-group-item.list-group-item-action
  %h5
    = team.name
    = link_to leave_team_path(team, user_id: current_user.id), method: :patch do
      %button.btn.btn-danger.float-right
        Leave team
  %p= team.description
  %ul
    - team.users.each do |user|
      %li= formatted_worker(user, user_path(user))
