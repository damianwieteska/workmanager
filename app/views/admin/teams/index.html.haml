%h1 Teams

.row
  .col
    %table.table.table-striped
      %thead.thead-dark
        %tr
          %th Name
          %th Description
          %th Durability
          %th
          %th
          %th

      %tbody
        - @teams.each do |team|
          %tr
            %td= team.name
            %td= team.description
            %td= team.durability.try(:humanize)
            %td= link_to 'Show', admin_team_path(team)
            %td= link_to 'Edit', edit_admin_team_path(team)
            %td= link_to 'Destroy', admin_team_path(team), method: :delete, data: { confirm: 'Are you sure?' }

    %br

    = link_to new_admin_team_path do
      %button.btn.btn-lg.btn-dark
        New
